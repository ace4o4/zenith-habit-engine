import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ace4o4.zenithhabitengine",
  appName: "Zenith Habit Engine",
  webDir: "dist/client",
};

export default config;
